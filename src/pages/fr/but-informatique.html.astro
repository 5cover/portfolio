---
import { detailHref } from '../../lib/links';
import { normalizeLocale } from '../../i18n/site';

const locale = normalizeLocale(Astro.currentLocale);

return Astro.redirect(detailHref(locale, 'history', 'history-but'));
---
