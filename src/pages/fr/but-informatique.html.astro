---
import { detailHref } from '../../lib/links';
import { normalizeLocale } from '../../i18n/site';

return Astro.redirect(detailHref(normalizeLocale(Astro.currentLocale), 'history', 'history-but'));
---
