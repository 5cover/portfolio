---
import { getCollection } from 'astro:content';
import Detail from '../../../infonodes/project/Detail.astro';
import { get } from '../../../lib/infonodes';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import type { Context } from '../../../lib/types';

export async function getStaticPaths() {
    return (await getCollection('project')).map(({ id }) => ({ params: { id } }));
}
const id = Astro.params.id;
---

<BaseLayout pageName={`project/${id}`}>
    {(c: Context) => <Detail {c} of={get.project(Astro.currentLocale, id)} />}
</BaseLayout>
