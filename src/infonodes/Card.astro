---
import type { CollectionKey } from 'astro:content';
import type { Infonode } from '../lib/content';
import type { PropsBase } from '../lib/types';

interface Props<C extends CollectionKey = CollectionKey> extends PropsBase {
    of: Infonode<C>;
}
// todo
const { of } = Astro.props;

switch (of.type) {
    case 'contact':
        of.type;
        of.data;
}
---
