---
import { normalizeLocale } from '../i18n/site';
import ButInformatiqueBody from './but/Body';
const locale = normalizeLocale(Astro.currentLocale);
const { default: copy } = await import(`./but/${locale}.tsx`);
---

<ButInformatiqueBody locale={locale} copy={copy}>
    <slot />
</ButInformatiqueBody>
<script src='../../src/scripts/but.ts'></script>
