---
import type { AstroComponentFactory } from 'astro/runtime/server/index.js';

interface Props {
  content: AstroComponentFactory;
  className?: string;
}

const { content: Content, className } = Astro.props;
const wrapperClass = className;

---
<div class={wrapperClass}>
    <Content />
</div>
