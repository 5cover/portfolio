---
import type { Infonode } from '../lib/content';
import type { PropsBase } from '../lib/types';
import LiteratureCard from './LiteratureCard.astro';
interface Props extends PropsBase {
    of: readonly Infonode<'literature'>[];
}

const { c, of } = Astro.props;
---

<ul class='lvl list-detail-split-card'>
    {
        of.map(entry => (
            <li>
                <LiteratureCard {c} of={entry} />
            </li>
        ))
    }
</ul>
