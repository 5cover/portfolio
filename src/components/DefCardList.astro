---
import type { Infonode } from '../lib/infonodes';
import DefCard from './DefCard.astro';

type Props = {
    defs: readonly Infonode<'def'>[];
};
const { defs } = Astro.props;
---

<ul class='lvl list-def'>
    {
        defs.map(def => (
            <li>
                <DefCard def={def} />
            </li>
        ))
    }
</ul>
